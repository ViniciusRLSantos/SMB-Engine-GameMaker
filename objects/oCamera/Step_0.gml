//zoom = lerp(zoom, zoom_to, 0.1);

//zoom_to += keyboard_check_pressed(vk_up);
//zoom_to -= keyboard_check_pressed(vk_down);

/*
if (target == noone) {
	if instance_exists(oPlayer) target = oPlayer;
}

var xTo = target.x;
var yTo = target.y-target.sprite_height/2;

//x += (xTo - x)/5;
//y += (yTo - y)/2;
x = xTo;
y = yTo;

